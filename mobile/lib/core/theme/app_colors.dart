import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF3B82F6);     // blue-500
  static const primaryDark = Color(0xFF1D4ED8); // blue-700
  static const accent = Color(0xFF6366F1);      // indigo-500

  // Gradients
  static const gradientStart = Color(0xFF1E3A8A); // blue-900
  static const gradientEnd = Color(0xFF312E81);   // indigo-900
  static const cardGradientStart = Color(0xFF1E40AF);
  static const cardGradientEnd = Color(0xFF4F46E5);

  // Background
  static const bg = Color(0xFFF1F5F9);          // slate-100
  static const surface = Colors.white;
  static const surfaceAlt = Color(0xFFF8FAFC);  // slate-50

  // Text
  static const textPrimary = Color(0xFF0F172A);  // slate-900
  static const textSecondary = Color(0xFF64748B); // slate-500
  static const textHint = Color(0xFFCBD5E1);     // slate-300

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Border
  static const border = Color(0xFFE2E8F0);      // slate-200
  static const borderFocus = primary;
}
