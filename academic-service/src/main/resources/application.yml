spring:
  application:
    name: academic-service
  datasource:
    url: jdbc:mysql://localhost:3306/academic_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

server:
  port: 8085
